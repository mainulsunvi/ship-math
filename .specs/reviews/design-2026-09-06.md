# Review — UI design upgrade (two Frontend passes), 2026-09-06

Change set (presentation-only claim): Pass 1 — `app/styles/brand.css` (NEW tokens),
`app/components/global/ShipMathPage.tsx` (NEW), `app/components/global/ShipMathNav.tsx`
(rewritten: brand row + Tabs, exact-match highlighting, dead class removed),
`app/routes/app.tsx` (NavMenu → Dashboard/Zones/Settings, brand.css linked),
`app/routes/app.additional.tsx` (redirect-only stub). Pass 2 — `app/routes/app._index.tsx`
(ShipMathPage wrap, layout reorg), `app/routes/app.zones.tsx` (ShipMathPage + New zone
primaryAction), `app/routes/app.settings.tsx` (rewritten placeholder, scaffold gone),
`app/components/rules/KindChip.tsx` (NEW), `app/components/ui/AccentChip.tsx` (NEW),
`app/components/ui/ModalSection.tsx` (NEW), `RulesTable` (Badge→KindChip, Behavior column),
`RuleEditorModal` + `ZoneEditorModal` (sections), `PostalRuleEditor` (accent preview chip).

Gates (orchestrator-run, authoritative): `tsc --noEmit` 0 · root vitest 11 files / **140/140**
(baseline 140 from the earlier criterion-6 pass; this change adds no tests and none dropped —
consistent with a behavior-frozen change) · `vite build` clean.

Every file above read in full, plus untouched banner-contract components
(`SyncReportWarnings`, `SyncStatusCard`) and `tsconfig.json`, `app/routes.ts`, PROGRESS.md.

## 1. Verdict: CHANGES REQUESTED

One required item; nothing BLOCKED. Re-review after the one-line fix lands; expected APPROVED.

- **REQUIRED — `app/components/global/ShipMathNav.tsx:50`: `const handleTabChange =
  (selectedTabIndex: number) => {…}` is an arrow at component scope.** This violates
  docs/INSTRUCTION.md §Coding Conventions ("full function declarations… avoid arrow functions
  unless the situation specifically calls for one" — a named handler passed to `onSelect` does
  not). Review 004-005 §3 flagged this exact arrow as "pre-existing — fix opportunistically";
  this change **rewrote the file**, so the pre-existing exemption no longer applies, and the
  same change meticulously uses declarations everywhere else (including inline `function`
  expressions in JSX props in every other touched file). Fix: replace with
  `function handleTabChange(selectedTabIndex: number) { … }` — one line, zero behavior change.

Should-fix (advisory, non-blocking) — see §3 items 2–3 and §2 notes.

## 2. Spec deviations found

| Location | Spec section | Deviation | Judgment |
|---|---|---|---|
| `ShipMathNav.tsx` (3 tabs) | 001 §7.6 "six sections" | Nav ships Dashboard/Zones/Settings; Simulator/Logs not present. | **Pre-existing settled state** — rules live on the Dashboard (settled in 005 impl), Simulator/Logs arrive with 008. Not chargeable to a presentation pass; active-state highlighting criterion IS satisfied (exact-match, below). |
| `app.additional.tsx` (redirect stub) | 001 §6 "repurpose/redirect… placeholder for now" | Kept as loader-redirect to `/app` with a delete-when-ready comment. | Accept — matches spec wording; bookmark-safe; `fs-routes` regenerates on delete. Zero references remain (grep: only its own comment + an unrelated word in a test doc-comment). |
| `app.settings.tsx` (placeholder) | 003/007 (Settings surface) | "General settings arrive with the next release." | Accept — 003/007 lanes pending; scaffold content removed, page renders under ShipMathPage. Parent `app.tsx` loader authenticates, so the leaf loader returning `{}` is safe. |

## 3. Convention violations

1. **`ShipMathNav.tsx:50` — arrow handler (REQUIRED — §1).**
2. **`app/styles/brand.css:12` (`--sm-accent-text: #4338ca`) + `AccentChip.tsx:25`** — the
   rgba overlays are dark-adaptive, but the chip **text** token is a fixed light-mode indigo.
   Contrast on dark admin surfaces ≈1.7–2.1:1 (#4338ca vs #303030/#1e1e1e), well under 4.5:1;
   the file-header claim "read acceptably on dark surfaces" doesn't hold for this token.
   Should-fix: add a `@media (prefers-color-scheme: dark)` variant (lighter indigo) — one
   token, keeps the rebrand contract.
3. **`app/settings.tsx:6-11`** — `export const loader = async function() {…}` / same for
   `action`: anonymous function expressions where every sibling route uses
   `export async function loader(…)`. Consistency nit (also a stray double blank line after
   imports). Fold into the same fix pass.

Clean on everything else: no arrow components anywhere (all four new components are `function`
declarations); modals-over-routes preserved (RuleEditorModal/ZoneEditorModal/delete confirms —
no new routes for flows); `useFetcher` for every mutation, unchanged; Polaris-only UI (the one
raw `<span>` in AccentChip exists solely to carry `--sm-*` vars that Polaris token props can't
express, and it wraps a Polaris `Text` — documented in the component header); no GraphQL
touched, so no query/mutation redefinition risk; component reuse is real, not nominal:
AccentChip ← KindChip **and** PostalRuleEditor; ModalSection ← RuleEditorModal **and**
ZoneEditorModal; ShipMathPage ← all three routes.

## 4. Missing coverage

None chargeable — this is a presentation-only pass against already-covered behavior
(004/005 criteria; their tests are the 140-suite and stay green). No new acceptance criteria
were introduced by this change, so none lack tests. The behavior contract was verified by
full diff-read instead (§5), which is the right tool for "no logic drift":

- **`app._index.tsx`** — all 10 intents (`sync`, `seed`, `testMode`, `rule-create/update/
  delete/duplicate/toggle/priority`, `set-evaluation-mode`) intact; both fetchers
  (`tableFetcher`, `modeFetcher`); banners: reply critical/success, sync-failure + **Retry
  sync**, `SyncReportWarnings`, stale warning, test-mode info, **≥400 soft-cap warning**
  (`RULES_CAP_WARN` logic untouched); **disabled Simulate rates + Tooltip** (span-wrapped,
  correct); evaluation-mode **Select** (modeFetcher) + **Behavior column** prop; pagination
  with loader-side out-of-range clamp; delete-confirm modal; load-bearing order
  (Prisma → ensure owner → push → audit) in every handler.
- **`app.zones.tsx`** — all 5 intents incl. bare `sync` retry; same banner set; `parseZoneForm`
  field-validation + `validatePostalRuleForCountries` backstop unchanged; zone→rule unlink
  semantics unchanged.
- **Modals** — client validation (name/priority/action Zod checks), close-only-on-`ok`,
  `sanitizeConditionsForKind` on kind switch, province/worldwide/postal state machines: all
  byte-identical in behavior.
- **Dead code** — `KIND_LABELS` exists only in `KindChip.tsx` (grep-verified); `quantible`,
  `AdditionalPage`, Additional NavMenu link: zero matches; **no unused imports** in any touched
  file (manually audited — `tsconfig.json` has no `noUnusedLocals`, so the tsc gate would not
  have caught one; Badge import in RulesTable is still legitimately used for the "stops" chip).
- **Accent restraint** — `--sm-*` consumed only by the 8px nav dot and AccentChip (grep: 3
  files); no large fills; backgrounds are rgba overlays.
- **Nav exact-match** — `/app`, `/app/zones`, `/app/settings` hit their tabs exactly;
  `/app/additional` never reaches the nav (server-side loader redirect) and would fall back to
  index 0 regardless — the greedy-prefix bug class is correctly avoided.

## 5. Positive notes

- `KIND_LABELS` extraction into KindChip removed the table/editor duplication at the source;
  the table cell and the editor title now cannot drift.
- ShipMathPage is a genuinely thin pass-through (props → Polaris Page, zero logic) — exactly
  what the reuse rule wants; all three routes now get consistent headers for free.
- The exact-match `findIndex` with explicit Dashboard fallback documents WHY exact matching is
  safe ("flows live in modals — no nested routes").
- The change kept hands off every `app/lib/` module — zone-matching/evaluator/engine consumers
  (carrier, simulator, Function parity) are untouched by construction; §A4 purity boundary
  unaffected.
- app.additional.tsx's self-documenting deletion note prevents a zombie route.
- Dark-mode thinking was present (rgba overlays), just incomplete on the text token (§3.2).

## 6. Standard scenarios (behavior — this change must not move them)

- **Uninstall → reinstall:** unchanged, verified no logic drift — webhooks, Prisma cascade,
  and `getOrCreateShop` untouched; nav/settings are pure UI.
- **Plan downgrade:** unchanged — nothing tier-gated in the touched files; the ≥400 soft-cap
  banner renders on the same condition as before.
- **Partial webhook failure / retry:** unchanged — no webhook code touched; the analogous
  metafieldsSet failure path (stale/critical banner + Retry intent on both routes) verified
  intact line-by-line.
- **Large catalog:** unchanged — 50/page pagination + out-of-range clamp intact; no catalog
  reads added or removed.

## 7. Conditions on this verdict

1. Convert `handleTabChange` to a function declaration (§3.1) — required for APPROVED.
2. Advisory: dark-mode `--sm-accent-text` variant (§3.2) and `app.settings.tsx` loader/action
   declaration style (§3.3) — fix in the same pass if convenient; not gating.

---

# Micro-review — Switch primitive + boolean-toggle conversion, 2026-09-06

Change set: `app/components/ui/Switch.tsx` (NEW), `app/styles/brand.css` (appended `.sm-switch*`),
`app/components/ui/SettingToggle.tsx` (Checkbox→Switch, props unchanged), swaps in
`RulesTable.tsx` (×1), `app/routes/app.zones.tsx` (×1), `ActionEditor.tsx` (×3),
`ZoneEditorModal.tsx` (×2). All files read in full; grep `<Checkbox|ToggleButton|pressed=` over
`app/`: zero matches. Gates (orchestrator-run): tsc 0 · vitest 140/140 · vite build clean.

## Verdict: APPROVED (this change only — parent §7.1 still open, see below)

1. **Logic drift: none.** All 8 sites are control swaps with byte-identical wiring —
   `onToggle(rule, next)` (RulesTable), `zone-toggle` payload `value: next ? "1" : "0"`
   (app.zones), `patchTier/patch({perItemEnabled|perWeightEnabled|openEnded: next})`
   (ActionEditor ×3), `handleWorldwide`/`setProvinces(next ? [WORLDWIDE] : [])`
   (ZoneEditorModal ×2). No loaders/actions/fetchers/`app/lib` touched.
2. **A11y: pass.** `role="switch"`, `aria-checked`, `aria-label` (label required, hideable),
   native button → Space/Enter, `:focus-visible` ring (`--p-color-border-focus`), disabled
   attr + aria-disabled + `:disabled` style. Redundant `!disabled` guard is harmless.
3. **Convention: pass.** Function declarations throughout (incl. inline named expressions);
   Switch is the single toggle primitive (grep clean); SettingToggle props contract unchanged →
   app._index (test mode), RuleEditorModal (stop on match), ZoneEditorModal (zone enabled)
   call sites untouched.
4. **CSS: pass.** Polaris tokens + `--sm-accent` only; dark-mode safe; geometry consistent
   (16px travel in 34px box). Sole fixed hex `#fff` knob-on-accent — accepted (native-switch
   convention). `all: unset` correctly re-establishes box/cursor/focus.
5. **Scenarios:** UI-only swap — uninstall/reinstall, plan downgrade, webhook retry, large
   catalog all unchanged by construction.

**Parent-review status:** advisory §3.2 (dark-mode `--sm-accent-text`) is now FIXED in
brand.css (media query present). **REQUIRED §7.1 is NOT fixed** — `ShipMathNav.tsx:89` still
`const handleTabChange = (selectedTabIndex: number): void => {`; parent verdict remains
CHANGES REQUESTED. Out-of-scope drift observed for the orchestrator: `ShipMathNav.tsx` changed
after the design review (prefix-fallback highlighting, `quantible-custom-max-width` wrapper,
blank line in Tabs JSX) without appearing on any change list — owner should surface it for
review.
